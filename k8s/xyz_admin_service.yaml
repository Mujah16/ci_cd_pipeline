apiVersion: v1
kind: Service
metadata:
  name: xyz-admin-service
spec:
  type: NodePort 
  selector:
    app: xyz-admin
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30080